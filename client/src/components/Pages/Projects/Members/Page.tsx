import React from 'react';
import { Project } from '@openapi';

type MembersPageProps = {
  project: Project
};

export const MembersPage = (props: MembersPageProps) => {

  return (
    <React.Fragment>
    </React.Fragment >
  );
}
