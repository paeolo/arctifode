import React from 'react';
import { Project } from '@openapi';

type TasksPageProps = {
  project: Project
};

export const TasksPage = (props: TasksPageProps) => {

  return (
    <React.Fragment>
    </React.Fragment >
  );
}
